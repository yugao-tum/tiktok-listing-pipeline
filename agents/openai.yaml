interface:
  display_name: "TikTok Listing Pipeline"
  short_description: "三产品并行、GPT图片审计、本地化与飞书回填流水线"
  default_prompt: "Use $tiktok-listing-pipeline to process the next three products, routing every image audit and analysis task through each product's ChatGPT conversation in the GPT in-app browser."
policy:
  allow_implicit_invocation: true
